class Courses {
    constructor(id, subscriberId, title, videoUrl) {
        this.id = id;
        this.subscriberId = subscriberId;
        this.title = title;
        this.videoUrl = videoUrl;
    }
} 

export default Courses;